{
  "name": "productlab-test-task",
  "version": "1.0.0",
  "description": "1",
  "main": "index.js",
  "scripts": {
    "install-all": "yarn install && yarn --cwd client install && yarn --cwd server install",
    "start-all": "concurrently \"yarn --cwd client dev\" \"yarn --cwd server start\""
  },
  "author": "Radomir R",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
